name: flutter_application_althea
description: "ALTHEA - Sistema de gestión y programación de citas médicas."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.8.1
  provider: ^6.1.2
  google_fonts: ^6.2.1
  intl: ^0.20.2
  supabase_flutter: ^2.12.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
